# Production Environment Variables for Alveo System
# IMPORTANT: Replace all placeholder values with actual production credentials
# NEVER commit this file with real credentials

# ============================================
# Application Ports
# ============================================
PORT=5001
VITE_APP_PORT=5002
VITE_WEB_PORT=5003

# ============================================
# Database Configuration
# ============================================
# IMPORTANT: Use strong password from secure vault (20+ characters)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public&sslmode=require
DATABASE_URL="postgresql://prod_user:REPLACE_WITH_STRONG_PASSWORD@db.internal:5432/dental_prod?schema=public&sslmode=require"

# Docker Compose variables (if using docker-compose.yml)
POSTGRES_DB=dental_prod
POSTGRES_USER=prod_user
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
POSTGRES_PORT=5432

# ============================================
# Redis Configuration
# ============================================
# IMPORTANT: Use strong password from secure vault (20+ characters)
REDIS_URL="redis://:REPLACE_WITH_STRONG_REDIS_PASSWORD@redis.internal:6379"
REDIS_PASSWORD=REPLACE_WITH_STRONG_REDIS_PASSWORD
REDIS_PORT=6379

# ============================================
# JWT Configuration
# ============================================
# IMPORTANT: Generate unique 64-character random strings
# Generate with: openssl rand -hex 32
JWT_SECRET="REPLACE_WITH_64_CHARACTER_RANDOM_STRING"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# ============================================
# Super Admin Setup
# ============================================
# IMPORTANT: Generate unique 32-character random string
# Generate with: openssl rand -hex 16
# SECURITY: Remove or change this after creating first super admin
SETUP_KEY="REPLACE_WITH_32_CHARACTER_RANDOM_STRING"

# ============================================
# Email Service (Resend)
# ============================================
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY="re_REPLACE_WITH_YOUR_RESEND_API_KEY"
EMAIL_FROM="Alveo System <noreply@yourdomain.com>"

# ============================================
# Environment Mode
# ============================================
NODE_ENV="production"

# ============================================
# CORS Configuration
# ============================================
# IMPORTANT: Must match your frontend URL EXACTLY (no trailing slash)
# DO NOT use "*" in production - specify exact domain
CORS_ORIGIN="https://app.yourdomain.com"

# ============================================
# Frontend Configuration
# ============================================
# IMPORTANT: Must be the public URL where your API is accessible
VITE_API_URL="https://api.yourdomain.com"

# Optional: URL where the app (panel) is accessible
VITE_APP_URL="https://app.yourdomain.com"

# ============================================
# Monitoring & Error Tracking (Optional)
# ============================================
# Sentry DSN for error tracking (optional but recommended)
# Get from: https://sentry.io/
# SENTRY_DSN="https://xxxxxxxxxxxxx@sentry.io/xxxxxxx"

# ============================================
# Security Notes
# ============================================
# 1. Generate all secrets with strong random values (see commands above)
# 2. Use different secrets for dev/staging/production
# 3. Store production secrets in a secure vault (AWS Secrets Manager, etc.)
# 4. Never commit this file with real values to version control
# 5. Rotate secrets regularly (quarterly recommended)
# 6. Ensure database uses SSL/TLS (sslmode=require in DATABASE_URL)
# 7. Remove or change SETUP_KEY after creating super admin
# 8. Verify CORS_ORIGIN matches frontend URL exactly

# ============================================
# Deployment Checklist
# ============================================
# [ ] All placeholder values replaced with real credentials
# [ ] All secrets are strong and unique (32+ characters)
# [ ] Database uses SSL/TLS connection (sslmode=require)
# [ ] Redis uses password authentication
# [ ] CORS_ORIGIN is specific (not "*")
# [ ] Email domain is verified in Resend
# [ ] SSL/HTTPS is configured
# [ ] Backups are configured and tested
# [ ] Monitoring is set up
# [ ] Error tracking is configured (Sentry)
